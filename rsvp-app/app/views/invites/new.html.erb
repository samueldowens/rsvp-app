<h1>New invite</h1>

<h2> Create Invite </h2>
<%= form_for @invite do |f| %>
    <%= f.select(:user_id, options_for_select(@users)) %>
    <%= f.hidden_field :event_id %>
    <%= f.hidden_field :rsvped %><br>
    <%= f.submit %>
<% end %>
<%= link_to 'Back', invites_path %>

